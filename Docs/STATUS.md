# Hermes Surface Dev：Windows 阶段状态

当前 Windows 源码版本为 **1.7.8**；历史 P8 总结见 [P8_FINAL.md](P8_FINAL.md)；上游起点 tag：`baseline-upstream-2026-09-23-69a43ae`。

## 1.7.8 编辑区符号与首行

源码编辑区关闭符号连字，标题输入框采用块级布局；空白便签进入编辑时复位首行。构建和定向检查已通过，真实字体显示仍待用户验收，详见 [RELEASE_1.7.8](RELEASE_1.7.8.md)。

## 1.7.7 悬停预览生命周期

- 原生鼠标位置兜底关闭；Windows 同时核对左键释放，避免漏掉 WebView leave/up 后预览常驻。
- 显示、悬停与关闭串行处理；关闭使旧会话失效，切换会话重置选择状态。
- 隐藏时卸载预览内容、停止正文刷新；hover 不再复制全文，窗口高度最多统计十行。
- 代码审查、验证与人工验收范围见 [RELEASE_1.7.7](RELEASE_1.7.7.md)。

## 1.7.6 完整预览与胶囊末端空间

- 移除悬停预览行数/字符限制，全文在预览内滚动阅读；任务写回比较完整正文。
- 拖动头与成员按原生计划的同一比例分配实际 viewport，避免固定 CSS 尺寸与窗口边界不一致导致最后成员被裁切。
- 保留 1.7.5 Editor，修复范围与验证记录见 [RELEASE_1.7.6](RELEASE_1.7.6.md)；原截图环境的 GUI 仍待用户复测。

## 1.7.5 Editor 架构纠偏

- 单一常驻 CodeMirror 文档负责源码编辑，MarkdownPreview 负责阅读排版；MarkdownSurface 通过 AST source positions 协调点击定位、选区与滚动锚点。
- 删除实验性容器前缀、富块替换与伪 WYSIWYG 装饰；阅读任务通过编辑器单字符事务写回既有保存管线。1.7.4 Capsule 实现完整保留。
- 编辑期间冻结派生阅读快照，保护 undo 与中文 IME；自动验证结果见 [RELEASE_1.7.5](RELEASE_1.7.5.md)。Windows GUI、实际滚动和 macOS 均为 **manual verification required**。
- 当前架构以 [EDITOR_ARCHITECTURE](EDITOR_ARCHITECTURE.md) 为准；下列旧版前缀/装饰描述仅为历史记录。

## 1.7.4 胶囊 viewport 与 Markdown 呈现修正

- 组布局改用逻辑尺寸，原生边界统一投影并补足小数 DPI 下的末端像素；普通组 viewport 不滚动，只有密集超容量组启用内部滚动。
- Markdown 容器标记归并为语义前缀，任务框取代所属列表标记的视觉槽。横线改为行级分隔线，裸 URL、代码高亮、图片受阻占位、标题与表格对齐补齐。
- 便签局部文字色按背景对比度计算，编辑器与只读 Markdown 共用。回归文本、测试及待人工验收范围见 [RELEASE_1.7.4](RELEASE_1.7.4.md) 与 [MANUAL_ACCEPTANCE](MANUAL_ACCEPTANCE.md)。

## 1.7.3 组布局与容器前缀重构

- Rust `capsule_layout` 对整条 monitor/side 求解；冲突组全局合并，固定成员槽位，超容量使用一个可滚动 viewport。颜色、expanded 状态不参与几何。
- `desktop/capsule_groups` 是运行时 snapshot/window registry；每个视觉组一个 WebView，复用 identity 和窗口池。Windows 在 DOM-ready 后使用一个 DeferWindowPos batch 交接；组拖动只移动一个 HWND，单成员拆出使用隐藏 surface 交接，drop 一次保存。
- Markdown 容器前缀消费 Lezer 节点，以 source-preserving mark + 绝对定位图形呈现；有序编号原样保留，实际字体测量决定软换行正文缩进。单文档、常驻 EditorView、IME 保护及外部 ChangeSet 保留。
- 构建与测试结果见 [RELEASE_1.7.3](RELEASE_1.7.3.md)。全部 GUI、混合 DPI 和 macOS 实测均为 **manual verification required**；编译结果不等于体验已验收。

## 1.7.2 常驻编辑与胶囊修复

- 常驻源码编辑器替代便签 textarea/预览的完整切换，点击定位、活动行语法显露、外部最小差异更新和滚动锚点保持；支持现有保存与冲突管线。技术边界见 [交互说明](INTERACTION_1.7.2.md)。
- 包含此前未提交的 1.7.1：持久颜色、展开保留胶囊、Windows 整组拖动。追加旧色一次迁移、12 色区分、轴线前端拖动头和保持拼接外轮廓的展开反馈。
- 预览任务乐观更新，保存通知合并核对，不重建窗口或重播入场动画。布局去除固定 sleep 补间，视觉反馈用 CSS；既有拖动期间原生采样保留。
- TypeScript、4 项前端源码/任务测试、8 项 Rust 胶囊测试及 Windows NSIS 生产构建通过；GUI、帧率与内存仍待实测，Mac 编译和实机验收未做。详见 [发布说明](RELEASE_1.7.2.md)。

## 1.7.0 交互整理

- 已实现并完成编译检查：便签正文单击进入同窗写作、Esc/失焦保存后回阅读；旧的显式切换按钮保留。胶囊单击直接展开，右键菜单可单独展开、编辑、关闭；会话保留非破坏性 `hidden`/`stored`/`expanded` 区别。
- 已实现并完成编译检查：单便签快捷键再次触发可选择收回胶囊或取消桌面固定；旧配置默认沿用收回。胶囊颜色键持久化，8 色低饱和色板；仅真正拖动的成员脱离拼接外观。
- 已实现并完成编译检查：预览按 owner 和会话识别 enter/leave，允许跨窗短暂间隙；正文复用安全 Markdown 管线、支持选区与任务源位置写回，超过预览长度时提示展开。协议说明见 [INTERACTION_1.7.0.md](INTERACTION_1.7.0.md)。
- 以上仅为代码与构建状态，Windows GUI、资源占用及 Mac 实机体验均待人工验证。连续胶囊原生窗口的位置变更仍由系统逐窗执行，拼接动画观感需实测。

## 已完成

- P0：核对 fork/上游与 HEAD；Windows 使用独立 bundle identifier、显示名、配置/数据目录；取消原版数据自动迁移、文件关联、默认快捷键及构建前 `taskkill`；Windows NSIS 基线构建通过。
- P1-A：停用启动时原版更新检查；隐藏更新/CDK 设置入口；移除自动更新命令和更新助手入口；dev/build 不再请求 GitHub 贡献者 API，关于页保留静态来源与许可说明。
- P1-B：Windows 主窗口不在静默启动时静态创建；便签池容量改为 0，闲置便签窗口关闭后不保留 WebView。外部文件的静默唤起路径已调整。
- P2：Windows Release 主程序与 NSIS 安装包前后大小已记录；类型检查、编译、Rust 148 项与前端 110 项测试通过。
- P3（Windows）：外部 Markdown 绑定持久化；可绑定多个目录（默认非递归，可选子目录）、创建目录内 `.md`、从列表固定为独立 Tile，并在原文件上编辑保存。Rust 文件监听按绑定 ID 通知相关窗口；自动扫描新增文件，排除已解绑文件。保存使用同目录临时文件和 Windows 原子替换，按内容 revision 检查冲突，本机保存草稿。
- P3 隔离 GUI 验证：在 `D:/pinNote/p3-gui-test` 绑定目录，打开并固定 `Today.md`，独立便签编辑内容成功写回同一原文件；使用外部原子替换后，便签自动显示新正文。未使用真实同步目录。TypeScript 检查、前端 110 项测试、Rust 153 项测试及 Windows NSIS 构建通过。
- P3 后续修复：外部文件便签关闭时保存并销毁原生窗口，避免透明窗口遮挡桌面；用户确认 1.2.3 的 pin 关闭与再次打开正常。
- P4 第一批：预览隐藏完整的文首 YAML Frontmatter，编辑和原文件保留原文；任务勾选按源位置改动单个 `[ ]`/`[x]`，支持重复任务。1.2.4 的相关测试、TypeScript 检查和 Windows NSIS 构建通过，GUI 尚待用户验收。
- P4 第二批：代码高亮、按需加载 Mermaid、外部文件相对图片、远程图片开关、列表延续和快捷格式化已进入 1.2.5；用户决定跳过 P4 GUI 验收并进入 P5。该决定不等于 P4 GUI 已通过。
- P5 Windows：主题 token、Tokyo Night/Everforest、逐字段全局与单便签覆盖、字体/行距/间距/圆角/边框/阴影/背景色、便签图片模糊和透明度、版本化 JSON 主题导入导出、Windows Acrylic 开关已实现。用户反馈 1.2.6 的背景实心、磨砂质感不符预期；后续版本按 Obsidian 参考图改为边栏冷色半透明、正文实心，并修正独立便签透明度默认值与背景图图层冲突。1.2.8 的 SetWindowRgn 原生裁剪导致独立便签出现白色矩形层和标题栏痕迹；1.2.9 移除该裁剪，改为 Windows 11 系统圆角提示、无边框窗口阴影和无边框色。原有无背景图时不创建模糊图层、避免重复写入样式变量的优化保留。用户确认 1.2.9 GUI 可以继续下一阶段。Mac 原生材质留待协作者。
- P6 Windows 核心（1.3.0）：每张内部或绑定的外部便签有独立本机会话，保存显示状态、启动策略、普通/置顶窗口模式、显示器工作区相对逻辑坐标、尺寸与 DPI 信息。移动/缩放后防抖保存，关闭和主动退出前补存。`--silent` 按设置恢复便签，正文加载后使用 Windows 不激活前台的显示调用；空恢复列表仅驻留托盘。单便签全局快捷键默认空，可录制、清除、恢复默认；注册冲突时保留旧键。应用级新增“打开笔记列表”全局键，保留既有新建和显示/隐藏键。Rust 与前端构建检查通过；GUI 留给用户。
- P6 Windows 修复（1.3.1）：用户发现开机静默启动后，用单便签快捷键关闭唯一的便签会让应用退出，快捷键无法再次恢复。Tauri 在最后一个窗口销毁时发送无退出码的 `ExitRequested`；现在 Windows 对这种隐式退出保持托盘进程和全局快捷键，托盘“退出”等显式退出仍生效。用户已确认 GUI 复测通过。
- P7-A Windows 桌面附着（1.4.0）：增加普通、置顶、桌面附着三种窗口模式。桌面模式把便签设为 Explorer 桌面图标宿主的子窗口，校验 DPI 模式，转换屏幕/父窗口坐标，编辑时临时脱离、手动保存后返回桌面。附着失败会返回错误，保留原会话模式。已做编译和类型检查；Win+D、普通应用遮挡、拖动缩放、Explorer 重启、多 DPI 和 Acrylic 组合待用户 GUI 验收。
- P7-A 后续修复（1.4.1）：桌面子窗口停用 DWM Acrylic、系统阴影与原生圆角，CSS 保留便签圆角和颜色，处理用户截图中的黑边。磁贴增加同窗阅读／写作按钮，切换时保留同一窗口的位置与尺寸，写作时可显示 Markdown 原文；增加锁定按钮，锁定后临时置顶并鼠标穿透，主界面逐便签设置可解锁。以上 GUI 行为待用户验收；原生 Acrylic 与桌面附着组合明确标记为不可用。
- P7-A 锁定交互修复（1.4.2）：Windows 锁定便签仍整窗鼠标穿透，但另建一个不含 WebView 的原生小窗口作为唯一可点的“解锁”按钮，随便签移动/缩放并在关闭时清理。主界面解锁保留为备用入口；按钮点击与实际穿透待用户 GUI 验收。
- P7-A 锁定按钮视觉修复（1.4.3）：Windows 解锁入口改为透明背景的开锁图标，对齐原锁图标位置；锁定时保留其他按钮的外观但它们随便签主体穿透。按钮外观和点击区域待用户 GUI 验收。
- P7-A 光标与锁图标修复（1.4.4）：阅读态正文显示可点击指针，写作态输入区保留文本指针。Windows 锁定后的原生解锁入口改用原有闭锁图形，并以窗口客户区定位，保持按钮尺寸和位置一致；待用户 GUI 验收。
- P7-A 锁按钮与失焦材质修复（1.4.5）：阅读态改为普通箭头；Windows 前端上报现有锁按钮的 DOM 矩形，原生解锁入口依该坐标定位并在缩放时更新。Windows 原生 Acrylic 失焦时系统会退为实色，非桌面附着的便签改在失焦时用原生 Blur、聚焦时恢复 Acrylic；实际视觉与性能待用户 GUI 验收。
- P7-A 材质回撤（1.4.6）：按用户要求撤回 1.4.5 的失焦 Blur 切换，恢复 Windows 原生 Acrylic 的默认聚焦/失焦行为；保留阅读态普通箭头和锁按钮 DOM 坐标定位。
- P7-B 初版（1.5.0，验收失败）：用户报告收纳界面不可用、贴边不准确、应用卡死且无法关闭。发现同步 IPC / UI 回调中创建 WebView2 的死锁路径、胶囊窗口事件权限遗漏，以及悬停反复调整同一窗口尺寸的交互问题。1.5.0 不应继续作为可用交付版本。
- P7-B 重做（1.5.1，GUI 待验收）：参照 [PaperTodo 用户手册第 5 节](https://github.com/snownico0722/PaperTodo/blob/main/doc/USER_GUIDE.md) 的贴边色条、悬停内侧预览、离开收回、点击原位恢复。常态为真正贴住物理屏幕边缘的 8 逻辑像素色条；独立共享预览展示多行正文和轻量标题/列表，移除浏览器 tooltip 与大块底部提示。建窗在后台串行执行；入口创建成功才销毁原便签，失败保留原窗口。增加最小事件权限，修正快捷键/托盘建窗路径。仍由用户做 GUI 验收，不将编译成功标作运行通过。

- P7-B 补充与 P8 Windows 收尾（1.6.0）：色条支持拖到左/右/顶部及跨屏，保存独立收纳坐标；便签增加轻微阴影。收纳列表不再读全部正文，文件监听空闲阻塞且事件簇最长合并 400ms，修正缺失字体引用。3 项收纳检查、TypeScript 与 Windows Release NSIS 构建通过。资源数据、未实现项和 A01–A20 状态详见 P8_FINAL.md；本轮 GUI/运行资源未验收。

## 待验收

- P0–P2 的新建、撤销/重做、拖拽缩放、托盘找回、导入副本等完整人工回归仍待验收；P3 GUI 仅验证了上述隔离闭环。
- 运行时 Working Set、Private Bytes、WebView2 子进程、隐藏窗口数及首次呼出延迟未取得可信数据。P2 资源和交互验收尚未通过。
- macOS 平台配置、构建工作流和 GUI 验证交由 Mac 协作者处理，交接见 [MAC_HANDOFF.md](MAC_HANDOFF.md)。当前 macOS 覆盖配置仍使用原版 identifier，不能把 Windows 隔离结果套用到 Mac。
- 主界面关闭到托盘时仍保留隐藏 WebView。此路径涉及现有保存与外部文件监听，本轮没有在缺少 GUI 验证时进一步销毁。
- P3 尚未实现应用内重命名、缺失文件重新关联和可靠识别外部改名；目录断连恢复、冲突交互、托盘退出时的未保存草稿需进一步 GUI 验收。文件 revision 比较与替换不是跨应用事务锁。
- P4 第二批的 GUI 项按用户决定跳过，后续如发现回归再针对性处理。
- P7-B GUI 待验收：左/右/顶部拖动收纳、同侧多便签排序与滚动、悬停不抢焦点、点击与单便签快捷键恢复原坐标/尺寸、外部 Markdown 更新、全局显示/隐藏、静默启动和多屏 DPI。普通关闭保持“隐藏”，不会生成胶囊。

## 范围

Windows 已完成 P3 文件绑定闭环、P4 代码交付、P5 外观与材质、P6 的静默恢复、逐便签布局和全局快捷键；P7-A 桌面附着与 P7-B 胶囊收纳已交付待 GUI 验收。P8 Windows 代码复查、性能调整、文档与安装包已交付；运行指标及完整验收仍待补齐，不能标记原计划全部通过。macOS 交接见 `MAC_HANDOFF.md`。
