# 首轮人工验收

## 1.7.3 Capsule Group / Markdown Prefix

状态：下列 GUI 项全部 **未测 / manual verification required**。由用户在虚构笔记/测试目录执行；build/test 不能替代 GUI 验收。

| 胶囊场景                                              | 必须满足                                                                                   |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| 单成员、两成员、三成员、四到六成员分别靠近            | 每个 visual group 一个可见 native surface；轮廓连续，无重叠、明显 1px seam、错位或不同尺寸 |
| 两组靠近，尤其 A=100/B=101/C=160 的归一化等价位置     | 全局合并/解碰撞；所有组有序且不越界                                                        |
| 拖动 grip 整组；快速按下/松开、Esc、跨屏、切换侧边    | 只移动一个组 surface；一次 drop 保存，顺序、颜色不变；取消恢复                             |
| 从组中分别拖出首/中/末成员；Esc 取消                  | 无 duplicate、ghost 或错误空白槽；旧 surface 与新 surface 明确交接                         |
| left / right / top；screen beginning / end            | grip 与成员共轴，组完整位于轴向工作区                                                      |
| 100% / 125% / 150% / 200% DPI                         | 固定等尺寸成员，组内无 seam；各档分别记录结果                                              |
| 双屏 mixed DPI；负坐标副屏；运行时改变 DPI/拔插显示器 | 正确恢复并使用目标 monitor 尺寸；无透明残留窗口                                            |
| expanded member、expanded → stored、重启 restore      | 成员不消失、不变槽位、不重排；颜色不变                                                     |
| 删除/隐藏首中末成员、增加成员、清空再恢复             | 归属与窗口生命周期正确，真正删除才释放颜色槽                                               |
| 右键 / hover preview；组内 A → B 连续悬停             | owner 平滑转移，无 dismiss/reopen 闪烁                                                     |
| 预览任务连续勾选、滚动、链接、选区/复制               | 保留 optimistic update、滚动与无白闪行为                                                   |
| 超过边缘容纳数量                                      | 单个有界组 viewport 可滚动，槽位不压缩；滚动后仍可展开/拖出                                |

Markdown 使用下面原文，并把窗口缩窄到多次软换行：

```markdown
- A

* B

- C

1. A
2. B
3. C

- [ ] A
- [x] B

- parent
  - child
    - 孙子

- [ ] parent
  - [ ] child

1. first
   1. nested
2. second

> Quote
>
> - [ ] nested task

- This is a very very very long list item that wraps to a second visual line and keeps wrapping at the body column.
- 中文长列表正文：反复输入中文内容并缩窄窗口，观察续行与正文起点是否对齐。
```

- 点击每个 item 正文中间，再 focus → blur → focus，连续几十次。marker 不独占一行，正文不被挤到下一行；active line 恢复真实源码，caret 保持命中的 source offset。
- 点击任务框优先只切换 `[ ]` / `[x]`；点击 prefix 附近不产生非法选区。有序编号 `10.` 始终保留源码。
- 长段落与嵌套续行对齐正文列；切换时没有明显 scroll jump/caret drift。
- Chinese IME 连续组词、撤销/重做、复制粘贴、Ctrl/Cmd+B/I/K、Tab、Enter 列表延续、图片粘贴/拖入、外部 `.md` 修改与冲突处理全部复核。
- 复杂块 image/math/table/Mermaid 保持既有管线；不把 rich block 切换问题混同前缀测试。

## 1.7.1 胶囊最短检查

使用虚构笔记测试；GUI 由用户执行，未测项保持“未测”。

| 场景                                     | 预期                                                                 |
| ---------------------------------------- | -------------------------------------------------------------------- |
| 连续收纳不同便签，展开、隐藏后再收纳     | 每张便签颜色保持；删除一张后，新便签可使用释放的颜色。               |
| 2–6 个近邻胶囊，拖动首成员旁的把手       | 整组随指针移动，松手后顺序和颜色不变；单击把手不展开便签。           |
| 展开组中头、中、尾成员，再收回           | 被展开成员胶囊仍在原位并有凸起标记；收回只消除标记，不重播整组入场。 |
| 拖组到左、右、顶部及混合 DPI 显示器      | 窗口贴边且布局补间顺畅；不出现透明区域遮挡或多余窗口。               |
| 展开成员的胶囊右键关闭、快捷键收回、重启 | 只影响对应便签；正文和外部文件不删除；原位置与颜色保留。             |

性能观察仍按下方方法一次记录窗口与进程数；不要把编译通过当作 GUI 验收。

## 1.7.0 最短检查

使用虚构内部笔记和仓库外测试 `.md`；GUI 由用户执行，未测项保持“未测”。

| 场景                                                    | 预期                                                                             |
| ------------------------------------------------------- | -------------------------------------------------------------------------------- |
| 2–6 个胶囊左/右/顶部拼接、单击和拖动                    | 单击直接展开；按下不拆开轮廓，超过阈值才移动；成员颜色换边、排序、重启后保持。   |
| 胶囊右键关闭组内成员和最后一个                          | 只隐藏所选入口，不删除正文/外部绑定；最后一个关闭后托盘仍可找回。                |
| 慢速及斜向跨到预览、A/B/C 连续切换                      | 不在间隙误收起；旧预览请求不覆盖新的；离开整体区域后收回。                       |
| 预览滚动、拖选复制、任务勾选、编辑入口                  | 正文不误展开；勾选只改原文件目标标记，冲突时提示；编辑在原便签窗口。             |
| 阅读单击、双击选词、拖选、链接/任务、Esc 与切到其他应用 | 专用控件不触发编辑；保存成功才回阅读，失败保留内容；中文输入法组合阶段不误结束。 |
| 快捷键选 store/hide，锁定/桌面层各测一次                | 关闭与收纳有区别；再触发可按原位展开，外部绑定和快捷键不丢。                     |
| 静默启动、外部文件原子替换、混合 DPI 和退出重启         | 旧布局/主题/绑定与新增选项保留；无多余透明窗口或残留预览。                       |

资源只做一次简短记录：1 张展开、3 张收纳、同一预览连续切换、全部关闭后的窗口数以及主进程和 WebView2 子进程数。人工报告可附截图；编译成功不等于上述 GUI 通过。

1.6.0 的最短 GUI 复核及最终 A01–A20 状态见 [P8_FINAL.md](P8_FINAL.md)，以下保留历次验收项目。

使用虚构测试目录，不选择原版花笺数据或真实同步目录。Windows 安装包位于 `src-tauri/target/release/bundle/nsis/`。先核对应用显示名为 Hermes Surface Dev，原版仍可独立运行。

1. 新建中文笔记，输入、撤销/重做、保存；关闭并重新打开确认正文与标题。
2. 同时打开两篇笔记，交替编辑并保存，确认内容没有串写。
3. 在编辑/预览/磁贴模式间切换，拖动与四角缩放；关闭后从托盘及原有快捷入口再次打开。
4. 编辑未保存内容时关闭便签或退出，确认内容已保存，或明确提示保存失败。
5. 准备一份测试用 `.md` 文件。在主界面点击“导入 Markdown”，编辑并保存导入后的笔记；确认原文件不变，笔记数据目录中的副本已更新。另将该文件从资源管理器拖入主窗口，编辑并保存；确认“外部文件”列表中的内容写回原文件。两条入口分别记录结果，不要把导入视为直接编辑原文件。
6. 分别记录仅托盘、1 张磁贴、3 张磁贴、全部关闭和退出后的窗口数、应用主进程及其 WebView2 子进程的 Working Set 与 Private Bytes。P0 对照构建需使用相同笔记、Release 设置和测试目录；未实测项保持“未测”。
7. P3：在仓库外虚构目录创建 `Today.md`，点击“绑定 Markdown 目录”，确认其出现在外部文件列表。选择该文件并钉屏；在磁贴右键菜单选择“转为小窗”，编辑并保存，确认原文件变化。关闭管理窗口后仍应能编辑磁贴。
8. 使用另一程序对 `Today.md` 做同路径原子替换，确认对应便签刷新；本地有未保存输入时，确认出现冲突选择且不会覆盖输入。新增 `.md` 应进入列表但不自动弹窗；解绑一个扫描文件、重新扫描及重启后仍应排除。分别试默认非递归与“含子目录”，并核对 UTF-8 BOM、CRLF 文件写回格式。
9. P4：在外部 `.md` 文首加入完整 YAML Frontmatter，后接正文分隔线、两条文字相同的任务和一段 `js` 代码；预览应隐藏 Frontmatter、保留正文分隔线并高亮代码。勾选第二条任务后查看原文件，确认只改第二个 `[ ]`。
10. P4：在绑定目录内放 `docs/Today.md` 与 `assets/a.png`，以 `../assets/a.png` 引图；预览应显示本地图片。再试越出绑定目录的图片路径，预览不得加载。远程 HTTPS 图片默认不显示，开启“允许远程图片”后才显示。
11. P4：加入 `mermaid` 代码围栏并切换预览，确认正常图表显示；输入错误图表时应只在该图表位置看到源码与错误。编辑有序/任务列表后按 Enter，确认新项继续且新任务未勾选；空项按 Enter 应结束列表。在主编辑器和独立便签中选中文字后试 Ctrl+B、Ctrl+I、Ctrl+K。
12. P5：切换浅色、Tokyo Night 和 Everforest，观察正文、预览、代码、Mermaid 与菜单；设置全局字号后只给一篇便签改字号，继续改全局颜色，确认该便签颜色仍跟随。选择本地便签背景图，调整图片模糊和背景透明度，文字应保持清晰；开关 Windows Acrylic 时观察主窗口的冷色半透明标题栏/侧栏与实心正文区，并把它和用户提供的 Obsidian 参考图对比。再观察独立便签是否真的透出并模糊桌面；重点看四角是否有矩形底层、白边或原生标题栏痕迹，在桌面和主窗口上方分别观察，并试一次缩放。状态文字只表示系统接受请求，不能代替视觉验收。关闭重开应用后确认设置保留，期间保存与外部文件监听仍正常。主题 JSON 导出再导入应还原可移植字段，本地背景图片不随主题文件导出。

不要把 `--silent` 的隐藏主界面写成已完成的边缘收纳功能。

## 1.7.2 必要人工验收（未测）

1. 用 `# Hello` / `This is **important text**.` 点单词中部，光标落在对应源码字符，其他行保持格式。
2. 第 50 行编辑后失焦、重新聚焦、再点击别段，确认源码光标/选区和可见段落连续；反复几十次无跳顶/重复监听。
3. 中文输入法组词时切换窗口，确认无丢字/重复字；检查撤销/重做、Ctrl/Cmd+B/I/K、列表 Enter、Tab、图片粘贴和拖入。
4. 阅读链接直接打开，编辑普通点击定位，Ctrl/Cmd+点击打开；任务勾选优先，正文空白可拖动窗口。
5. 预览滚动到任务并勾选：只更新任务，不消失、不闪白、不回顶。内部、外部文件各测一次，外部并发变化仍提示冲突。
6. 两张旧同色胶囊升级后区分明显；展开/收回/重启颜色不变，删除便签后新便签可复用槽位。12 色用满后均衡复用。
7. 左右组头在上端、顶部组头在左端，整组可拖动；单独拖出成员时不带组头残影。展开保留边缘胶囊，轮廓不歪。
8. 混合 DPI、跨屏、屏幕首尾位置、桌面附着、锁定、启动恢复和快捷键仍需实机回归。运行帧率/CPU/内存未测。
